# AVC 2019

`Stuctures`
> data centric objects
> build(attributes)
> load(attribute)
> get(attribute)
> mutate(attribute, as)
> dump()

`Processes`
> runtime objects
> boot()
> stop()

`Services`
> globally accessible helper objects
> service.configReader()

`Tests`
> assert()

`Commands`
> cli prompted commands
> run()